# Copy to keystore.properties for local builds only.
# CI uses env vars: KEYSTORE_FILE, KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD.
# Never commit the real keystore.
storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=termuxglibc
keyPassword=CHANGE_ME
